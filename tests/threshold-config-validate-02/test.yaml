requires:
    min-version: 7

command: |
  ${SURICATA_BIN} -v --set threshold-file="${TEST_DIR}/threshold.config" -l ${OUTPUT_DIR} -c ${SURICATA_YAML} -S ${TEST_DIR}/test.rules -r ../detect-pcrexform-01/input.pcap

checks:

    #- shell:
    #    args: grep -e "SC_ERR_PCRE_MATCH" suricata.log | wc -l | xargs
    #    expect: 1

    - shell:
        args: grep -e "Threshold config parsed.*0 rule.*found" suricata.log | wc -l | xargs
        expect: 1
