requires:
    min-version: 8.0.1

pcap: ../entropy-01/input.pcap

checks:
    - shell:
        args: grep "stacktrace:" suricata.log | wc -l | xargs
        expect: 0

    - filter:
        count: 1
        match:
          event_type: http
          metadata.entropy.content: 4.137370175000773
